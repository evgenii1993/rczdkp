export let getArrayFields = (arr, callBack) => {
    let obj = {};
    console.log();
    /*arr.map(currentValue => {
        return obj[Array.from(currentValue.matchAll(/\((.*?)\)/g))[0][1]] = currentValue.split("(")[0]
    })*/

    return obj["_error"] = "asdasdasd"
}

